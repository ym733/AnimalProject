@using Animal.Web.MediaComponents


@using (Html.BeginForm("DeleteCategoryType", "CategoryType", FormMethod.Post, new { enctype = "multipart/form-data", onSubmit = $"formSubmit('{Url.Action("DeleteCategoryType", "CategoryType")}', this, event);" }))
{
	@Html.AntiForgeryToken()
	<div>@Html.ValidationMessage("FormValidation")</div>

	@Html.SingleStringInput("id")

	@Html.SubmitButton("Submit form")
}
